package mna.thedarkenginej.engine.world.actor.state.visual;

import mna.thedarkenginej.engine.world.common.Range;

// TODO: multi-level visual range + fuzzy set ranges
public record VisualRange(Range range) {
}
